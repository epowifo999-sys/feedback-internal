# 项目开发规范

## 核心原则
每次完成任何功能开发或问题修复后，必须先完成自验流程，
确认无误后才能告知用户完成。禁止让用户通过刷新页面来
发现问题。

## 一、接口 / 后端自验
- 用 curl 调用修改的接口，打印完整返回结果
- 确认返回 JSON 格式正确、无报错字段
- 检查 server 启动日志，确认无异常输出
- 如有报错继续排查修复，直到接口返回正常结果

## 二、前端 / UI 自验
- 检查浏览器控制台是否有 JS 报错（通过 server 日志判断）
- 检查修改的 CSS class 是否有全局污染风险（是否影响其他页面或组件）
- 检查本次改动涉及的所有相关区域代码，确认未被意外影响

## 三、告知用户时的格式
完成自验后，按以下格式告知：

**已完成：** [简述做了什么]

**自验结果：**
- 接口测试：✅ 正常 / ❌ 异常（已修复）
- 控制台报错：✅ 无 / ❌ 有（已修复）
- 关联影响排查：✅ 无影响

**建议重点检查：** [列出视觉或交互层面需要用户确认的区域，
如无则写"无"]

## 四、改动影响范围评估
每次修改前，先列出本次改动可能波及的区域：
- 涉及全局样式（如修改了公共 class）→ 列出所有用到该
  class 的页面或组件
- 涉及公共接口或数据结构 → 列出所有调用该接口的前端模块
- 涉及数据库字段或表结构 → 确认相关查询和写入逻辑同步更新

## 五、禁止行为
- 禁止说"你可以刷新页面查看效果"作为验证方式
- 禁止在未完成自验的情况下告知用户已完成
- 禁止只修复表面报错而不排查根本原因
- 禁止修改公共组件或全局样式后不做影响范围评估

## 六、浏览器截图验证
开发环境已配置 SKIP_AUTH=true，无需登录即可访问所有页面。

凡涉及 UI 样式、交互、布局的改动，完成后必须：
1. 确认本地服务已启动（localhost:3001）
2. 用浏览器工具打开对应页面截图
3. 截图确认改动效果符合预期后才能告知用户完成
4. 若截图发现问题，继续修复直到视觉效果正确

注意：截图验证仅适用于开发环境，生产环境 SKIP_AUTH 不存在，
登录验证正常生效。
